import styled from "styled-components";
import Palette from "../../../assets/palette";

export const SectionTitle = styled.h2`
  grid-area: tskr-section-title;

  width: max-content;
  height: min-content;

  font-size: 24px;
  font-weight: 700;
  color: ${Palette.white};
`