import styled from "styled-components";

export const Container = styled.div`
  grid-area: tskr-date;

  display: inline-flex;
  flex-wrap: nowrap;
  align-items: center;
  height: fit-content;
  gap: 6px;
`